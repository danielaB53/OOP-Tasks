#pragma once

class Message {
public:

	Message(int data);
	const int data;
	
	//static Message& getInstance(int data);

//private:

	//Message(const Message& other) = default;
	//Message& operator=(const Message& other) = default;

	//~Message() = default;
};